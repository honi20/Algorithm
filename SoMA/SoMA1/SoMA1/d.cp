#include <iostream>
#include <vector>
#include <cmath>
#include <string>
using namespace std;
typedef long long ll;

int main(){
    cin.tie(0); cout.tie(0);
    ios::sync_with_stdio(false);
    
    int n, m;
    string str;
    cin>>n>>m>>str;
    // 5525
    
    for (int i=0; i<str.size(); i++){
        if (str[i] == 'I'){
            
        }
    }
    
    return 0;
}
